import { StyleSheet } from 'react-native';

export const homeSectionStyles = StyleSheet.create({
  heading: {
    fontSize: 17,
    fontWeight: '600',
    color: '#1e293b',
    marginBottom: 12,
  },
});
